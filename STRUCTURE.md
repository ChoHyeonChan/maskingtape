# 폴더 구조 가이드 — 기능(도메인)별로 나눈다

> **팀원에게**: AI(Claude Code·Cursor·ChatGPT 등)로 작업할 때 이 파일을 [CLAUDE.md](CLAUDE.md)·[CONTRIBUTING.md](CONTRIBUTING.md)와 함께 읽히고 "이 구조 규칙대로 작업해줘"라고 지시하세요.

## 왜 이렇게 하나 — 심사 배점과 직결

2차 심사는 **경력직 심사위원**이 보고, 「개발 문서 구체성」·「관리체계」에 배점이 있습니다. 폴더 트리만 봐도 **"이 팀이 뭘 어떻게 설계했는지"가 한눈에 읽혀야** 좋은 인상을 줍니다.

## 핵심 원칙 (딱 3개)

1. **코드가 길어져서 나누는 게 아니라, 기능(도메인)이 다르면 폴더가 다르다.**
   - ✗ "파일이 300줄 넘으니까 대충 쪼갠다"
   - ✓ "역할·도메인이 다르니까 폴더로 묶는다"
2. **폴더 이름 = 그 안이 뭘 하는지의 지도.** 폴더명만 봐도 내용이 예상돼야 합니다.
3. **"파일 1개 = 역할 1개"는 그대로 유지** — 파일을 쪼개는 게 아니라, 관련된 파일들을 **폴더로 묶는** 겁니다.

## 좋은 예시 — 우리 레포에 이미 있습니다

| 파트 | 구조 | 나눈 기준 |
|---|---|---|
| **apps/web** (프론트) | `components/{help, input, layout, results}` | 화면 **역할**별 |
| **apps/desktop** (Flutter) | `lib/{models, services, screens, widgets}` | **레이어**별 |
| **packages/core** (엔진) | `detectors/{identity, contact, financial, personal}` | 개인정보 **도메인**별 |

예를 들어 `core`의 탐지기는 개인정보보호법 분류에 맞춰 묶었습니다:
```
detectors/
  identity/    # 고유식별정보 — rrn.py (주민등록번호)
  contact/     # 연락처 — phone.py, email.py
  financial/   # 금융정보 — creditcard.py
  personal/    # 인적·신상 — name.py, name_llm.py, address.py
```
→ 폴더만 봐도 "우리가 어떤 개인정보를 다루는지"가 읽힙니다.

## 네 파트는 어떻게? (파트별 힌트)

| 파트 | 담당 | 상태 · 힌트 |
|---|---|---|
| `apps/web` | 수연 / plana | ✅ **이미 모범** (`components`를 역할별로). 새 화면도 같은 방식으로 폴더 추가 |
| `apps/desktop` | 준형 | ✅ **이미 레이어별로 잘 됨**. 유지 |
| `packages/core` | 현찬 | ✅ 도메인별 완료 |
| `apps/api` | 기태 | `routers/`는 나뉘어 있음. `schemas.py`가 커지면 `schemas/{scan, anonymize}` 처럼 **엔드포인트 도메인별**로 분리 검토 |
| `bench` | 서연 | `generator/`는 잘 나뉨. 루트의 평가 스크립트들(`evaluate`, `evaluate_masking`, `mask_quality`, `confidence_analysis`, `compare_name_detectors`)을 `evaluators/` 폴더로 묶으면 "평가 도구 모음"이 한눈에 보임 |

> **아직 파일이 몇 개 안 되면 억지로 나누지 마세요.** 5~6개를 넘어 "폴더로 묶으면 더 읽히겠다" 싶을 때가 신호입니다.

## AI에게 시킬 때 — 복붙용 지시문

```
이 저장소의 STRUCTURE.md·CONTRIBUTING.md·CLAUDE.md를 먼저 읽고, 그 규칙대로 작업해줘.
- 기능을 추가할 때 파일을 그냥 늘어놓지 말고, 관련된 것끼리 기능(도메인)별 폴더로 묶어줘
  (코드 길이가 아니라 "역할이 다르면 폴더가 다르다" 기준)
- "파일 1개 = 역할 1개" 규칙은 유지하고, 폴더로 묶기만 해
- 파일을 옮길 땐 git mv로 이력을 보존하고, import 경로가 깨지지 않게 정리해줘
  (패키지 __init__에서 re-export하면 바깥 코드는 안 바뀜)
- 옮긴 뒤 반드시 테스트를 돌려서 "하나도 안 깨졌다"를 확인해줘 (순수 구조 변경이니까)
```

## 옮길 때 주의 (안전하게 리팩토링)

1. **`git mv`로 옮긴다** — 커밋 이력이 보존됩니다.
2. **import 경로 정합성** — 파일을 옮기면 그걸 부르던 곳이 깨집니다. 패키지의 `__init__`에서 **re-export**하면 바깥에서 쓰는 공개 API(`from ... import X`)는 그대로 유지됩니다.
3. **순수 구조 변경이면 로직은 0** — 옮기고 나서 **테스트 개수가 그대로**여야 안전하다는 증거입니다. 하나라도 깨지면 경로가 덜 정리된 것.
4. **과분할 금지** — 파일 1개짜리 폴더를 남발하면 오히려 "오버엔지니어링"으로 보입니다. 3~5개 정도의 의미 있는 그룹으로.

---

> 참고: `packages/core`의 도메인 폴더 구조화가 실제 예시입니다 — PR을 보면 "파일 이동 + import 정리 + 테스트 그대로 통과"가 어떻게 되는지 볼 수 있습니다.
