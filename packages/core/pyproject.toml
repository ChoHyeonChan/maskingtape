[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "maskingtape"
version = "0.1.0"
description = "한국어 개인정보 비식별화 엔진 — 규칙 + 로컬 LLM 하이브리드 PII 탐지·마스킹"
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.10"
authors = [{ name = "Team maskingtape" }]
keywords = ["korean", "pii", "privacy", "anonymization", "deidentification", "masking"]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3",
  "Topic :: Security",
]

[project.optional-dependencies]
# ruff 0.16의 새 규칙(TRY004 등)이 기존 코드 린트를 깨 CI가 실패한다. CI·로컬을 같은
# 규칙으로 맞추는 임시 상한 — main 공통 문제이며 근본 정리는 별도 이슈(#60)에서.
dev = ["pytest>=8", "ruff>=0.4,<0.16"]

[project.scripts]
maskingtape = "maskingtape.cli:main"

[tool.setuptools.packages.find]
include = ["maskingtape*"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
testpaths = ["tests"]
